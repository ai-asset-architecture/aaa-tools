# v2.0.0 Release Closure Status

## Scope

This note records the current public release-closure status for `v2.0.0`.

## Release Alignment

- README install pin: `v2.0.0`
- Current packaged CLI version: `2.0.0`
- `CHANGELOG.md` includes the `v2.0.0` public baseline
- GitHub Release tag: `v2.0.0`

## Verification Command

```bash
./scripts/release-verify.sh v2.0.0
```

## Current Result

- Run date: `2026-06-03`
- Status: `blocked`
- Failure: installed `aaa` entrypoint aborts in a clean virtualenv because the tagged `v2.0.0` `pyproject.toml` does not declare all runtime dependencies used by the packaged CLI surface

Observed error excerpt:

```text
ModuleNotFoundError: No module named 'pydantic'
```

## Root Cause

The current `main` branch declares `pydantic` and `mcp` in `pyproject.toml`, but the historical `v2.0.0` tag does not. That means release verification from the tagged source fails even though the README install pin and changelog already point at `v2.0.0`.

## Next Action

- keep issue `#4` open as a release-discipline blocker
- decide whether to publish a corrective `v2.0.1` or other follow-up release
- rerun `./scripts/release-verify.sh <new-tag>` once the tagged dependency metadata is aligned
## Boundary

This note documents release-alignment evidence and the current blocker. It does not claim that `v2.0.0` is fully verified from a clean install path.
